// Must match app/src/lib/waiver.ts's WAIVER_VERSION exactly (the app repo). A contact only
// counts as "signed" here if they accepted this exact version — bump both together when the
// waiver text changes.
export const WAIVER_VERSION = '2026-08-13-v1';
